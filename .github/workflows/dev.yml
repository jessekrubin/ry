name: dev

on:
  push:
    branches:
      - "*"
      - "*/*"
      - "**"
      - "!main"
      - "!master"
    tags:
      - "*"
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: install-rust-stable
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          components: clippy,rustfmt
      - name: setup-rust-cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
      - uses: taiki-e/install-action@7572810d7dd469b651bb7793945692cf78da5dd7 # v2
        with:
          tool: just
      - run: just ci

  test-python:
    name: test-python-${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        target: [ x86_64 ]
        python-version: [ "3.11", "pypy3.11", "3.12", "3.13", "3.14", "3.14t" ]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: install-rust-stable
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - name: setup-rust-cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          prefix-key: "v0-rust-test-python-${{ matrix.python-version }}"
      - name: install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          python-version: ${{ matrix.python-version }}
      - name: install deps
        run: |
          uv sync
      # - name: install ry
      #   run: |
      #     uv -v pip install -e .
      #   env:
      #     RUST_BACKTRACE: 1
      - name: install ry
        run: |
          uv run maturin develop --profile fast-build
        env:
          RUST_BACKTRACE: 1
      - run: uv run python -m ry
      - run: uv pip freeze
      - run: uv run pytest tests

  test-python-cov:
    name: test-python-cov-${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        target: [ x86_64 ]
        python-version: [ "3.12" ]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # nightly
        with:
          components: llvm-tools-preview
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - name: install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          python-version: ${{ matrix.python-version }}
      - name: install deps
        run: |
          uv sync
      - run: rustc --version --verbose
      - run: uv pip freeze
      - name: Run coverage
        run: |
          source <(cargo llvm-cov show-env --sh)
          export CARGO_TARGET_DIR=$CARGO_LLVM_COV_TARGET_DIR
          export CARGO_INCREMENTAL=1
          cargo llvm-cov clean --workspace
          cargo test --workspace
          uv run -- maturin develop --uv
          uv run -- pytest tests --cov=ry --cov-report xml:coverage.xml
          COV_PACKAGES=(
            -p ry
            -p ryo3
            -p ryo3-aws-lc
            -p ryo3-brotli
            -p ryo3-bytes
            -p ryo3-bzip2
            -p ryo3-cookie
            -p ryo3-core
            -p ryo3-dev
            -p ryo3-dirs
            -p ryo3-flate2
            -p ryo3-fnv
            -p ryo3-fspath
            -p ryo3-glob
            -p ryo3-globset
            -p ryo3-heck
            -p ryo3-http
            -p ryo3-ignore
            -p ryo3-jiff
            -p ryo3-jiter
            -p ryo3-json
            -p ryo3-macro-rules
            -p ryo3-memchr
            -p ryo3-pydantic
            -p ryo3-quick-maths
            -p ryo3-regex
            -p ryo3-reqwest
            -p ryo3-same-file
            -p ryo3-serde
            -p ryo3-shlex
            -p ryo3-size
            -p ryo3-sqlformat
            -p ryo3-std
            -p ryo3-tokio
            -p ryo3-tokio-rt
            -p ryo3-tokio-websockets
            -p ryo3-twox-hash
            -p ryo3-ulid
            -p ryo3-unindent
            -p ryo3-url
            -p ryo3-uuid
            -p ryo3-walkdir
            -p ryo3-which
            -p ryo3-zstd
          )
          cargo llvm-cov report "${COV_PACKAGES[@]}" --lcov --output-path coverage.lcov
      - uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7
        # allow the action to fail bc idgaf about codecov but its nice to see (yolo)
        continue-on-error: true
        with:
          # files: coverage.lcov,coverage.xml
          files: coverage.lcov
          disable_search: true
          verbose: true
          name: cov-python-${{ matrix.python-version }}
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: jessekrubin/ry
